name: safe_chat
description: A secure way to chat

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^4.0.1
  get: ^4.6.5
  font_awesome_flutter:
  dio:
  connectivity:
  provider:
  jwt_decode:
  image_picker:



dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/jpg/